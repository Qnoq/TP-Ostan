{% extends 'base.html.twig' %}

{% block title %}Formulaire de contact{% endblock %}

{% block content %}

<h1>Formulaire de contact</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Envoyer</button>
    {{ form_end(form) }}
        <a href="{{path ('advice_post')}}">Retour à l'accueil</a>

{% endblock %}

