
{% extends 'base_backend.html.twig' %}

{% block title %}Administration
{% endblock %}
{% block content %}

    <h1>Les articles</h1>
    <a type="button" href="{{path ('backend_advicePostNew')}}" class="btn btn-primary d-block" role="button"><i class="fas fa-plus"></i>Ajouter un article</a>
   
   
        {% for post in posts %}
         
                    <h3>{{post.title}}</h3>
                
                    <p>{{post.description|truncate(300, true)}}</p>
                </td>
                <td>
                    {#<a href="{{path ('backend_advicePostEdit', {'id': advicePost.id})}}">Voir</a>#}
                    <a href="{{path ('backend_advicePostEdit', {'slug': post.slug})}}">Editer</a>

                    {# pour avoir la fonction supprimer, ne pas mettre de bouton mais directement l'include de la fonction delete #}
                    {{include('backend/post/_delete.html.twig')}}
                </td>
            </tr>
        {% endfor %}
        <button href="{{path ('backend_advicePostNew')}}" class="btn btn-primary d-block" role="button"><i class="fas fa-plus"> </i> Ajouter un article</button>
   
   
{% endblock %}

