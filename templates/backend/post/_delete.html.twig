
<form method="post" action="{{ path('backend_advicePostDelete', {'id': post.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cet article ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn btn-danger"><i class="far fa-trash-alt"></i> Supprimer</button>
</form>