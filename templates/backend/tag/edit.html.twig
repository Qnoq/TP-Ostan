{% extends 'base.html.twig' %}

{% block title %}Modifier un Tag{% endblock %}

{% block content %}

<style>
    .content {
        margin: 1em auto;
        width: 55%;

    }
</style>

<section class="section-post">

    <h1 class="text-center font-weight-bold mb-5">Modifier un Tag</h1>

    {{ include('backend/tag/_form.html.twig', {'button_label': 'Mettre à jour le tag'}) }}

    <div class="d-flex justify-content-center">
        {{ include('backend/tag/_delete.html.twig') }}
    </div>

    <div class="d-flex justify-content-center">
        <a href="{{ path('backend_tagList') }}" class="btn btn-primary">Retour à la liste</a>
    </div>

    
{% endblock %}