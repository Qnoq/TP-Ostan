
<form method="post" action="{{ path('backend_user_delete', {'slug': user.slug}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cet user ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.slug) }}">
    <button class="btn btn-danger"> <i class="far fa-trash-alt"></i> Supprimer l'utilisateur</button>
</form>