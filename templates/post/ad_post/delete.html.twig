
<form method="post" action="{{ path('adDelete', {'slug': post.slug}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer cette annonce ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ post.id) }}">
    <button class="btn btn-danger mx-2"><i class="far fa-trash-alt"></i> Supprimer mon annonce</button>
</form>